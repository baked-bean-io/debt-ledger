{
  "name": "debt-ledger",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "action",
    "plugin"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "yarn workspaces foreach -At run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "yarn@4.17.0"
}
